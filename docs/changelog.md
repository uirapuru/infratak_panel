# Infratak Project Changelog

Timeline of all code, configuration, and documentation changes. Auto-generated by AI assistants (mandatory discipline).

---

## 2026-04-14 (naprawa DELETE 405 w EasyAdmin 5)

### Fix: EasyAdmin 5 DELETE action zwraca 405 — brakujące askConfirmation()

**Summary:** Kliknięcie "Delete" w panelu admina zwracało 405 Method Not Allowed. EasyAdmin 5 renderuje przycisk DELETE z atrybutem `data-ea-action-url`. Bez `data-action-confirmation` JS klikając przycisk robi `window.location = deleteUrl` (nawigacja GET), zamiast pokazać modal i wysłać POST z CSRF tokenem.

**Root cause:** `action_group.html.twig` dodaje `data-ea-action-url` do przycisków. JS handler ignoruje elementy z `data-action-confirmation` i pozwala modalowi je obsłużyć. Bez `askConfirmation()` element NIE ma tego atrybutu → klik → GET → 405.

**Files Modified:**
- `src/Controller/AdminServerCrudController.php` — dodano `->update(Crud::PAGE_INDEX, Action::DELETE, fn => $action->askConfirmation())` i analogicznie dla `PAGE_DETAIL`

**Rationale:** `askConfirmation()` dodaje `data-action-confirmation="true"`, co powoduje że JS klik otwiera modal potwierdzenia, a po confirm wysyłany jest POST przez ukryty formularz `#action-confirmation-form` z tokenem CSRF.

---

## 2026-04-14 (naprawa produkcji — landing_nginx crash loop)

### Fix: lazy DNS resolution w landing.conf — admin_nginx upstream

**Summary:** `landing_nginx` wpadał w pętlę restartu z błędem `host not found in upstream "admin_nginx"`. Nginx rozwiązuje hosty upstreamów statycznie przy parsowaniu konfiguracji. Ponieważ `admin_nginx` może startować po `landing_nginx`, DNS Docker (127.0.0.11) nie znał jeszcze tego hosta i kontener kończył się z kodem 1.

**Files Modified:**
- `docker/nginx/conf.d/landing.conf` — zmieniono `proxy_pass http://admin_nginx:80` na `set $admin_upstream http://admin_nginx:80; proxy_pass $admin_upstream;` — wymuszenie lazy resolution przez istniejący resolver 127.0.0.11

**Rationale:** Zmienna w `proxy_pass` odkłada rozwiązanie DNS do czasu rzeczywistego żądania. Bez tej zmiany kolejność startu kontenerów determinuje powodzenie deployu.

**Działania produkcyjne przy tym incydencie:**
- Migracja `Version20260414090000` była już częściowo zaaplikowana na produkcji (`owner_id` istniało) — oznaczono ją ręcznie jako executed: `doctrine:migrations:version --add`
- Uruchomiono pozostałe 2 nowe migracje (Version20260414130000, Version20260414150000)
- Wyczyszczono cache Symfony dla obu ról (landing + admin)

---

## 2026-04-14 (BUG-009 — portal rotation + multi-tenancy final)

### Naprawa BUG-009: usunięcie rotacji hasła portalu OTS

**Summary:** Próba rotacji hasła na domenie portalu (`portal.{subdomain}.infratak.com`) przez `OtsApiClient` powodowała wyjątek — portal to oddzielna aplikacja Flask (port 5000, SQLite) bez endpointu `/api/login`. Wyjątek był re-throw'owany, co uniemożliwiało zapis `otsAdminPasswordCurrent` w DB. Kolejne retry używały nieaktualnego `oldPassword` i główna rotacja OTS też zaczynała failować. Oba serwisy miały nieprawidłowe hasło bez możliwości powrotu przez panel.

**Files Modified:**
- `src/Service/Provisioning/AwsProvisioningClient.php` — usunięto rotację portalu; `rotateOtsAdminPassword` wywołuje `OtsApiClient` tylko dla domeny głównej OTS
- `src/Service/Provisioning/AwsProvisioningClientInterface.php` — dodano parametr `portalDomain` do sygnatury (kompletność interfejsu, nie używany przy rotacji)
- `src/MessageHandler/RotateAdminPasswordHandler.php` — przekazuje `$server->getPortalDomain()` do metody (nieużywane, ale zachowuje zgodność interfejsu)
- `docs/bugs.md` — dodano BUG-009 z opisem, recovery przez SSM i statusem Naprawiony
- `docs/operations-runbook.md` — sekcja 7 przepisana; dodano tabelę architektury serwisów na instancji i procedurę recovery SSM

**Rationale:** Portal (`/opt/opentak-onboarding-portal/`, Flask + gunicorn) nie posiada własnej tabeli haseł — nie obsługuje rotacji przez API. Jedynym serwisem z `/api/login` + `/api/password/change` jest OTS na porcie 8081.

---

### Multi-tenancy: ROLE_USER widzi szczegóły serwera z ograniczonymi polami i dane OTS

**Summary:** Rozszerzono widok ROLE_USER o stronę szczegółów serwera (wcześniej była zablokowana). Użytkownik widzi domenę, status, subskrypcję i dane logowania do OTS — bez pól technicznych (step, awsInstanceId, owner, lastRetryAt, runtimeHours, logów operacyjnych). Dodano przycisk "Pokaż szczegóły" w liście serwerów dla ROLE_USER.

**Files Modified:**
- `src/Controller/AdminServerCrudController.php`:
  - `configureActions()` — dla ROLE_USER: wyłączono EDIT/DELETE/NEW, dodano `showDetail` (action DETAIL z etykietą "Pokaż szczegóły") i `resetAdminPassword`
  - `configureFields()` — konwersja do `yield`; pola techniczne (name, step, awsInstanceId, owner, lastRetryAt, runtimeHours, logi) osłonięte `if ($isAdmin)`
  - Pole OTS credentials: tabela z loginem `administrator`, hasłem z przyciskiem kopiowania i linkiem "Otwórz panel OTS" — widoczna dla wszystkich użytkowników
- `docs/ai-current-state.md` — zaktualizowano tabelę multi-tenancy

**Rationale:** Klient potrzebuje dostępu do danych OTS, żeby zalogować się do swojego serwera. Dane techniczne (AWS instance ID, step provisioning, itp.) nie są dla klienta relevantne i mogłyby być mylące.

---

### Rejestracja: zezwolenie na istniejący email (dodaj serwer do konta)

**Summary:** Jeśli przy rejestracji przez `/zamow/rejestracja` podany email już istnieje w bazie, zamiast błędu system dodaje nowy serwer do istniejącego konta (bez zmiany hasła). Klient z wieloma zamówieniami korzysta z jednego konta.

**Files Modified:**
- `src/Controller/OrderController.php` — lookup emaila przed walidacją; jeśli user istnieje — pomijany jest krok tworzenia i walidacja hasła; serwer jest tworzony i przypisywany do istniejącego konta

**Rationale:** Klient może zamawiać kolejne serwery bez zakładania nowego konta za każdym razem.

---

## 2026-04-14 (kody promocyjne)

### Rejestracja: blokada bez płatności + kody promocyjne

**Summary:** Rejestracja nowego konta wymaga teraz kodu promocyjnego. Kod określa czas działania serwera (domyślnie 1 dzień). Serwer automatycznie dostaje subskrypcję na ten czas.

**Files Modified:**
- `src/Entity/PromoCode.php` (nowy) — encja kodu: code, durationDays, maxUses, usedCount, expiresAt, isActive
- `src/Repository/PromoCodeRepository.php` (nowy) — `findValidByCode()` (case-insensitive, sprawdza aktywność i datę)
- `migrations/Version20260414150000.php` (nowy) — tabela `promo_code`
- `src/Controller/OrderController.php` — dodano walidację kodu, wywołanie `SubscriptionService::purchase()` po rejestracji, `durationDays` w sesji sukcesu
- `templates/order/register.html.twig` — nowa sekcja z polem "Kod promocyjny"
- `templates/order/success.html.twig` — informacja o przyznanym czasie działania
- `src/Controller/AdminPromoCodeCrudController.php` (nowy) — CRUD do zarządzania kodami (tylko ROLE_ADMIN)
- `src/Controller/AdminDashboardController.php` — dodano "Promo codes" do menu
- `docs/changelog.md` — ten wpis

**Rationale:** Tymczasowe zastąpienie bramki płatności — tylko posiadacze kodu mogą się zarejestrować.

---

## 2026-04-14 (multi-tenancy — ograniczenie widoku ROLE_USER)

### Panel: ROLE_USER widzi tylko swoją listę serwerów i akcję reset hasła

**Summary:** Użytkownik z rolą ROLE_USER ma dostęp do panelu, ale widzi wyłącznie swoje serwery bez strony szczegółów i logów. Jedyna dostępna akcja to "Reset admin password" (tylko dla READY serwerów).

**Files Modified:**
- `src/Controller/AdminServerCrudController.php` — `configureCrud()` usuwa defaultRowAction dla non-adminów; `detail()` blokuje bezpośredni dostęp URL; `configureActions()` dla non-adminów wyłącza DETAIL/EDIT/DELETE/NEW i zostawia tylko `resetAdminPassword`
- `docs/changelog.md` — ten wpis

**Rationale:** Ochrona danych innych użytkowników; ROLE_USER nie powinien widzieć logów operacyjnych (które mogą ujawniać dane infrastruktury) ani akcji administracyjnych.

---

## 2026-04-14

### Playwright E2E: testy ścieżki zamówienia serwera

**Summary:** Dodano 12 testów E2E pokrywających pełną ścieżkę zamówienia serwera.

**Files Modified:**
- `tests/e2e/order.spec.ts` (nowy) — 12 testów w 3 grupach: wybór serwera, walidacja formularza, szczęśliwa ścieżka

**Pokryte scenariusze:**
- Strona `/zamow` ładuje się i zawiera kartę OpenTAK; kliknięcie "Dalej" przenosi do formularza
- Formularz zawiera wszystkie pola; live preview domeny pojawia się po wpisaniu subdomeny
- JS filtruje niedozwolone znaki w polu subdomeny na bieżąco
- Walidacja: puste pola, hasło za krótkie, nieprawidłowy format subdomeny (inject przez `evaluate`)
- Pełna ścieżka → sukces: adres serwera, portal, e-mail logowania, hasło widoczne raz
- Po odświeżeniu strony sukcesu dane sesji są usunięte → redirect na `/zamow`
- Duplikat e-maila i duplikat subdomeny zwracają odpowiednie błędy inline

---

### Ścieżka zamówienia serwera: wybór → rejestracja → sukces

**Summary:** Dodano pełną ścieżkę zamówienia serwera ATAK. Przycisk "Chcę serwer ATAK" na stronie głównej prowadzi do wyboru typu serwera, po kliknięciu "Dalej" otwiera się formularz rejestracyjny (imię, nazwisko, telefon, e-mail, hasło, subdomena). Po wysłaniu: tworzony jest użytkownik z profilem, tworzony i uruchamiany jest serwer, strona sukcesu pokazuje adres serwera + dane logowania do panelu.

**Files Modified:**
- `src/Controller/OrderController.php` (nowy) — trasy `GET /zamow`, `GET|POST /zamow/rejestracja`, `GET /zamow/sukces`
- `src/Entity/User.php` — dodano pola `firstName`, `lastName`, `phone` (nullable) z getterami/setterami
- `migrations/Version20260414130000.php` (nowy) — kolumny `first_name`, `last_name`, `phone` w tabeli `admin_user`
- `templates/order/select.html.twig` (nowy) — wybór typu serwera
- `templates/order/register.html.twig` (nowy) — formularz rejestracyjny z live preview domeny
- `templates/order/success.html.twig` (nowy) — podziękowanie z adresem serwera i danymi logowania
- `templates/home.html.twig` — przycisk "Chcę serwer ATAK" w sekcji hero

**Rationale:**
- Zamówienie nie wymaga jeszcze bramki płatniczej — bramkę można dołączyć między wyborem a rejestracją bez zmiany istniejących tras. Hasło panelu pokazywane raz na stronie sukcesu. Hasło admina OTS pojawi się w panelu po zakończeniu provisioningu.

---

### MVP abonamentów serwerów — 50 PLN za dzień

**Summary:** Dodano bazowy mechanizm abonamentów przypiętych do serwera i użytkownika. Abonament kupowany jest na liczbę dni po 50 PLN/dzień, zapisuje historię zakupu, wydłuża `subscriptionPaidUntil`, a scheduler egzekwuje lifecycle: po wygaśnięciu zatrzymuje instancję, po 30 dniach bez odnowienia kolejkuje cleanup. Odnowienie zatrzymanej instancji kolejkuje start.

**Files Modified:**
- `src/Entity/Server.php` — dodano `owner`, `subscriptionPaidUntil`, `subscriptionExpiredAt`, `subscriptionTerminationQueuedAt`
- `src/Entity/ServerSubscription.php` — nowa encja historii zakupu abonamentu z ceną 50 PLN/dzień
- `src/Repository/ServerSubscriptionRepository.php` — repozytorium zakupów abonamentu
- `src/Repository/ServerRepository.php` — zapytania do oznaczania wygasłych abonamentów i 30-dniowego grace period, także dla już zatrzymanych serwerów
- `src/Service/Billing/SubscriptionService.php` — logika zakupu/odnowienia abonamentu i wznowienia zatrzymanego serwera
- `src/Command/EnforceSubscriptionsCommand.php` — komenda `app:subscriptions:enforce`
- `src/State/CreateServerProcessor.php` i `src/Service/Server/ServerCreationService.php` — przypisanie ownera przy tworzeniu serwera przez zalogowanego użytkownika
- `src/Controller/AdminServerCrudController.php` — akcja `Add subscription` w detalu serwera i pola abonamentowe
- `src/Controller/AdminServerSubscriptionCrudController.php` — podgląd historii zakupów abonamentów
- `src/Controller/AdminDashboardController.php` — pozycja menu `Subscriptions`
- `templates/admin/add_subscription.html.twig` — formularz zakupu abonamentu w panelu
- `migrations/Version20260414090000.php` — migracja pól i tabel abonamentowych
- `compose.yaml`, `compose.prod.yml` — scheduler uruchamia również `app:subscriptions:enforce`
- `docs/ai-current-state.md` — opis aktualnego przepływu

**Rationale:**
- MVP świadomie nie integruje jeszcze bramki płatniczej; daje spójny model domenowy i operacyjny lifecycle abonamentów. Płatność można później podpiąć jako źródło wywołania `SubscriptionService::purchase()`.

---

### Naprawa BUG-007 i BUG-008: log rotacji hasła + korekta timestampów w sync

**Summary:** Dwie poprawki wykryte podczas przeglądu zmian Codex.

**Files Modified:**
- `src/MessageHandler/RotateAdminPasswordHandler.php` — dodano `ServerOperationLog` przy sukcesie i błędzie rotacji (BUG-007); import `ServerOperationLog`
- `src/Command/SyncServerStatusCommand.php` — korekta na READY teraz ustawia `startedAt=now` i `clearEndedAt=true`; naprawiono mapowanie `stopping` → null zamiast STOPPED (BUG-008)
- `docs/bugs.md` — dodano BUG-007 i BUG-008 z opisem i statusem Naprawiony

**Rationale:**
- BUG-007: rotacja hasła OTS była niewidoczna w panelu admina (tylko logger). Teraz każda rotacja tworzy wpis w logu operacyjnym.
- BUG-008: serwer zsynchronizowany do READY miał stary `endedAt` z poprzedniego stopu oraz brak `startedAt`. Po poprawce timestamps są spójne z nowym cyklem uruchomienia.

---

### Przegląd i naprawa zmian od Codex: stopping→STOPPED i deleteViaGet

**Summary:** Przegląd wszystkich niezacommitowanych zmian. Znaleziono i poprawiono dwa błędy wprowadzone przez Codex.

**Files Modified:**
- `src/Command/SyncServerStatusCommand.php` — `stopping` mapowany na `null` (pomiń) zamiast `STOPPED` (stan przejściowy EC2, nie końcowy)
- `src/Controller/AdminServerCrudController.php` — usunięto zbędną metodę `deleteViaGet` (GET mutation, niezabezpieczona CSRF, nieużywana)
- `.env` — przywrócono pusty `APP_SECRET=` (Codex wpisał wartość do tracked pliku)

**Rationale:**
- `stopping` to stan przejściowy — nie należy go traktować jako `STOPPED` i zapisywać do bazy zanim EC2 ukończy operację.
- GET mutation (`deleteViaGet`) to antywzorzec — mutacje przez GET są podatne na CSRF i sprzeczne z HTTP semantyką.

---

### Akcja ręcznej zmiany hasła OTS z detalu serwera

**Summary:** Dodano akcję `Change admin password` w widoku detail serwera EasyAdmin. Akcja otwiera formularz wpisania nowego hasła administratora OpenTAK/OTS, waliduje CSRF, minimalną długość i potwierdzenie, a następnie zleca istniejący `RotateAdminPasswordMessage` do workera provisioningowego.

**Files Modified:**
- `src/Controller/AdminServerCrudController.php` — dodano akcję detail `Change admin password`, trasę `GET/POST /admin/admin-server/{entityId}/change-admin-password`, walidację formularza i dispatch `RotateAdminPasswordMessage(origin: manual-change)`
- `templates/admin/change_ots_admin_password.html.twig` — nowy formularz zmiany hasła z detalu serwera
- `docs/changelog.md` — ten wpis

**Rationale:**
- Operator może teraz ustawić konkretne hasło administratora OTS z poziomu detalu serwera, zamiast używać wyłącznie resetu z losowo generowanym hasłem. Zmiana nadal przechodzi przez worker i zapisuje hasło w `otsAdminPasswordCurrent` dopiero po sukcesie rotacji.

---

## 2026-04-13 (4)

### Stały podgląd hasła administratora OTS w detalu serwera

**Summary:** Zmieniono zachowanie po rotacji hasła OTS: aktualne hasło administratora pozostaje zapisane w `otsAdminPasswordCurrent` i jest stale widoczne w detalu serwera EasyAdmin. Usunięto logikę jednorazowego reveal przez flash/pendingReveal.

**Files Modified:**
- `src/Controller/AdminServerCrudController.php` — dodano pole `OpenTAK admin password` i `OpenTAK admin password rotated at` w widoku detail; usunięto czyszczenie `otsAdminPasswordPendingReveal` przy wejściu w detal; zaktualizowano komunikat manualnego resetu
- `src/MessageHandler/RotateAdminPasswordHandler.php` — po sukcesie rotacji czyści `otsAdminPasswordPendingReveal` i polega na trwałym `otsAdminPasswordCurrent`
- `templates/bundles/EasyAdminBundle/crud/detail.html.twig` — dodano obsługę kopiowania hasła w detalu serwera
- `docs/operations-runbook.md` — zaktualizowano opis dostępności hasła
- `docs/ai-current-state.md` — zaktualizowano opis przepływu rotacji hasła

**Rationale:**
- Operator chce mieć aktualne hasło administratora OTS dostępne stale w panelu, a nie tylko jednorazowo po zakończeniu rotacji. Pole `otsAdminPasswordCurrent` już istnieje w bazie, więc zmiana nie wymaga migracji.

---

### Korekta Route53 Zone ID — infratak.com vs calbal.net

**Summary:** Zweryfikowano w AWS Route53, że `Z0843124BD4D2BF9GZ0S` jest strefą `infratak.com.`, a `Z08501132MVTT1RZZYKRL` jest strefą `calbal.net.`. Poprzedni wpis miał te wartości odwrócone, co powodowało `InvalidChangeBatch: RRSet is not permitted in zone calbal.net` przy tworzeniu rekordów `*.infratak.com`, a potem błąd certbota `NXDOMAIN` dla `grzesio.infratak.com`.

**Files Modified:**
- `.env.deploy` — `AWS_ROUTE53_HOSTED_ZONE_ID` ustawione na `Z0843124BD4D2BF9GZ0S` (strefa `infratak.com.`)
- `.env.infra` — `ROUTE53_ZONE_ID` ustawione na `Z0843124BD4D2BF9GZ0S`
- `.env.infra.example` — to samo
- `.env.deploy.example` — to samo

**Rationale:**
- Domena jest konfigurowalna przez `APP_BASE_DOMAIN`; Route53 musi wskazywać hosted zone tej samej domeny. Dla `infratak.com` poprawny hosted zone id to `Z0843124BD4D2BF9GZ0S`.

---

## 2026-04-13 (3)

### Live status + log viewer w panelu administracyjnym

**Summary:** Dodano automatyczne odświeżanie statusu serwerów w widoku listy i detalu co 3 sekundy bez przeładowania strony. Na liście: hover nad wierszem pokazuje tooltip z ostatnimi 5 zdarzeniami. Na detalu: pełny log operacji (tabela) poniżej pól serwera, aktualizowany na żywo.

**Files Modified:**
- `src/Controller/AdminServerCrudController.php` — dodano dwa endpoint JSON: `GET /admin/api/servers` (lista + ostatnie 5 logów per serwer) i `GET /admin/api/servers/{id}` (pełny log); dodano import `ServerOperationLogRepository`, `ServerRepository`, `JsonResponse`
- `src/Repository/ServerOperationLogRepository.php` — dodano `findRecentForServer()` i `findAllForServer()`
- `templates/bundles/EasyAdminBundle/crud/field/choice.html.twig` — dodano `data-field-property="{{ field.property }}"` na badge statusu/stepu (cel dla JS)
- `templates/bundles/EasyAdminBundle/crud/index.html.twig` (new) — override z JS: polling co 3s, live update badge statusu/stepu, floating tooltip z ostatnimi logami przy hover
- `templates/bundles/EasyAdminBundle/crud/detail.html.twig` (new) — override z tabelą logów operacji i JS polling co 3s; tabela: czas, level, status, step, wiadomość, kontekst

**Rationale:**
- Admin widzi aktualny stan bez ręcznego odświeżania strony — kluczowe podczas monitorowania provisioningu.
- Tooltip na liście pozwala szybko sprawdzić ostatnie zdarzenia bez wchodzenia w detal.
- Endpointy pod `/admin/api/` są chronione tym samym access control co panel (wymaga `ROLE_ADMIN`).

---

## 2026-04-13 (2)

### Synchronizacja stanu serwerów z AWS (scheduler)

**Summary:** Dodano automatyczną synchronizację stanu serwerów w DB z rzeczywistym stanem instancji EC2 na AWS. Nowa komenda `app:servers:sync-status` sprawdza co 5 minut serwery w statusie `READY`/`STOPPED` i koryguje rozbieżności. Jeśli instancja nie istnieje już na AWS, serwer jest oznaczany jako `DELETED`.

**Files Modified:**
- `src/Service/Provisioning/AwsProvisioningClientInterface.php` — dodano metodę `getInstanceState(string $instanceId): string`
- `src/Service/Provisioning/AwsProvisioningClient.php` — `getInstanceState` zmieniona z `private` na `public`
- `src/Repository/ServerRepository.php` — dodano `findSyncable(): array` (serwery z `awsInstanceId` w statusie `READY`/`STOPPED`)
- `src/Command/SyncServerStatusCommand.php` (new) — komenda synchronizacji; mapuje `running`→`READY`, `stopped`→`STOPPED`, `terminated`→`DELETED`; `InvalidInstanceID.NotFound` traktowane jako `terminated`
- `compose.yaml` — dodano serwis `scheduler` (pętla co 300 s)
- `compose.override.yaml` — dodano `APP_ENV=dev` dla schedulera
- `compose.prod.yml` — dodano serwis `scheduler` z `env_file: .env.deploy`

**Rationale:**
- Bez synchronizacji zmiany stanu instancji poza panelem (AWS Console, awaria, zewnętrzne zatrzymanie) nie były nigdy widoczne w DB. Po naprawie DB zawsze odzwierciedla aktualny stan — max. 5 minut opóźnienia.
- Synchronizacja jest bezpieczna: działa tylko na serwerach w stabilnym stanie (`READY`/`STOPPED`), omija przejściowe stany AWS (`pending`) i serwery aktywnie provisionowane.

---

## 2026-04-13

### Naprawa BUG-001: odświeżenie CSRF token po POST /api/login w OtsApiClient

**Summary:** Po `POST /api/login` Django może zmienić cookie `csrftoken`. Kod odczytywał `X-CSRFToken` ze starego `GET /api/login` zamiast z zaktualizowanych cookies, powodując `403 CSRF verification failed` przy `POST /api/password/change`.

**Files Modified:**
- `src/Service/Provisioning/OtsApiClient.php` — po scaleniu cookies z odpowiedzi POST /api/login odświeżenie `$csrfToken` z `$cookies['csrftoken']` jeśli Django zwróciło nowy token; fallback na oryginalną wartość
- `docs/bugs.md` — status BUG-001 zmieniony na Naprawiony

**Rationale:**
- Django REST Framework rotuje `csrftoken` cookie przy zmianie sesji (login). Bez odświeżenia tokenu żądanie zmiany hasła dostawało 403, blokując całą rotację. Naprawa jest minimalna: jedna linia po `array_merge` cookies, z bezpiecznym fallbackiem na stary token jeśli `csrftoken` nie pojawił się w odpowiedzi.

---

### Naprawa BUG-004 + BUG-005 + BUG-006

**Summary:** Trzy poprawki zbiorcze: zachowanie hasła OTS przy re-provisioningu, eliminacja duplikatów FAILED w logach cleanup, ujednolicenie parametrów Doctrine.

**Files Modified:**
- `src/Service/Server/ServerCreationService.php` (BUG-004) — `initializeForProvisioning` zachowuje `otsAdminPasswordCurrent` gdy serwer był już rotowany (`otsAdminPasswordRotatedAt !== null`); dla świeżego serwera zachowanie bez zmian (`'password'`)
- `src/MessageHandler/DeleteServerHandler.php` (BUG-005) — usunięto `dispatchProjection` z bloku catch; przy nieudanym cleanup błąd jest rejestrowany przez logger a wyjątek re-rzucany do retry przez RabbitMQ bez tworzenia zduplikowanych wpisów FAILED w `ServerOperationLog`
- `src/Repository/ServerRepository.php` (BUG-006) — `hasActiveServerWithName`: zmieniono `ServerStatus::DELETED` na `ServerStatus::DELETED->value` (spójność z resztą repozytorium)
- `docs/bugs.md` — statusy BUG-004, BUG-005, BUG-006 zmienione na Naprawiony

**Rationale:**
- BUG-004: Re-provisioning istniejącego serwera resetował hasło do 'password', co powodowało nieudaną rotację po-provisioningową jeśli OTS miał już inne hasło. Teraz `otsAdminPasswordCurrent` jest zachowywane jako `oldPassword` dla następnej rotacji.
- BUG-005: Każdy z 5 retry cleanup tworzył osobny wpis FAILED w logach operacyjnych. Usunięcie projection z catch zostawia logi czyste — błędy widoczne przez `worker_provisioning.log`, stan `CLEANUP` w adminie sygnalizuje trwającą operację.
- BUG-006: Kosmtyczna spójność — Doctrine ORM 3.x obsługuje oba warianty, ale jednolity styl redukuje ryzyko przy przyszłych refaktoringach.

---

### Naprawa BUG-003: clearSleepAt — czyszczenie sleepAt po stop/start

**Summary:** Pole `sleepAt` pozostawało ustawione w bazie na starą datę po każdym zatrzymaniu lub uruchomieniu serwera, bo `ServerProjectionMessage` nie miał mechanizmu czyszczenia tego pola. Stary timestamp mógł też dopasować zduplikowany `StopServerMessage` z kolejki i spowodować nieoczekiwane zatrzymanie serwera.

**Files Modified:**
- `src/Message/ServerProjectionMessage.php` — dodano pole `clearSleepAt: bool = false`
- `src/MessageHandler/ServerProjectionHandler.php` — dodano obsługę `clearSleepAt` (wywołuje `setSleepAt(null)`)
- `src/MessageHandler/StopServerHandler.php` — `clearSleepAt: true` przy sukcesie stopu; dodano parametr do prywatnej `dispatchProjection`
- `src/MessageHandler/StartServerHandler.php` — `clearSleepAt: true` przy sukcesie startu; dodano parametr do prywatnej `dispatchProjection`
- `src/MessageHandler/ManualStopServerHandler.php` — `clearSleepAt: true` przy sukcesie ręcznego stopu; dodano parametr do prywatnej `dispatchProjection`
- `docs/bugs.md` — status BUG-003 zmieniony na Naprawiony

**Rationale:**
- Scheduled stop jest jednorazowy — po wykonaniu harmonogram powinien zostać wyczyszczony. Manual start/stop anuluje harmonogram. Bez czyszczenia `sleepAt` pole zostawało w bazie na czas nieokreślony, stwarzając ryzyko zduplikowanych stopów i dezinformując admina w UI. Naprawa jest niedestruktywna: domyślna wartość `false` zachowuje istniejące zachowanie wszystkich wywołań które nie powinny czyścić pola.

---

### Naprawa BUG-002: hasło OTS pokazywane dopiero po sukcesie rotacji

**Summary:** Ręczny reset hasła w adminie pokazywał nowe hasło w flash natychmiast przy kliknięciu przycisku — zanim worker zdążył wykonać rotację przez OTS API. Jeśli rotacja się nie powiodła, admin widział hasło, które nigdy nie zostało ustawione.

**Files Modified:**
- `src/Controller/AdminServerCrudController.php` — usunięto `addPasswordFlash` z hasłem; zastąpiono prostym komunikatem informującym, że hasło pojawi się w detalu po zakończeniu
- `src/MessageHandler/RotateAdminPasswordHandler.php` — usunięto warunek `origin === 'manual-reset'`; `otsAdminPasswordPendingReveal` jest teraz zawsze ustawiany na `$message->newPassword` po sukcesie, niezależnie od origin
- `docs/bugs.md` — status BUG-002 zmieniony na Naprawiony

**Rationale:**
- Mechanizm `pendingReveal` (istniejący dla post-provisioningu) jest właściwym miejscem do jednorazowego pokazania hasła — gwarantuje, że admin widzi je wyłącznie po potwierdzonym sukcesie rotacji przez workera. Ujednolicono zachowanie dla obu origin (`manual-reset` i `post-provisioning`).

---

### Analiza błędów + dokumentacja znalezisk (docs/bugs.md)

**Summary:** Przeprowadzono pełną analizę kodu źródłowego pod kątem błędów produkcyjnych. Utworzono plik `docs/bugs.md` z 6 udokumentowanymi błędami, w tym 2 krytycznymi. Dodano plik `CLAUDE.md` z dokumentacją projektu dla przyszłych instancji Claude Code.

**Files Modified:**
- `docs/bugs.md` (new) — katalog znanych błędów: BUG-001..BUG-006 z opisem, lokalizacją i propozycją naprawy
- `CLAUDE.md` (new) — dokumentacja projektu: komendy, architektura, reguły, zmienne środowiskowe

**Rationale:**
- BUG-001 (stary CSRF token w OtsApiClient) jest najgroźniejszym kandydatem na błąd produkcyjny blokujący rotację hasła OTS — wymaga pilnej naprawy.
- BUG-002 (hasło w flash przed sukcesem rotacji) powoduje sytuację, gdzie admin widzi hasło, które nigdy nie zostało ustawione.
- BUG-003/004 to błędy logiczne w cyklu życia serwera (sleepAt, re-provisioning).
- Dokumentacja w bugs.md zapewnia traceability dla przyszłych napraw.

---

## 2026-04-02

### Playwright E2E Setup + Registration Bug Fix

**Summary:** Added complete Playwright E2E testing framework (Node.js + TypeScript), fixed HTTP 500 error in user registration when email transport unavailable, updated documentation with new mailer safeguards.

**Files Modified:**
- `package.json` (new) — Playwright + npm scripts
- `playwright.config.ts` (new) — E2E configuration
- `tests/e2e/home.spec.ts` (new) — landing page smoke test
- `tests/e2e/register.spec.ts` (new) — registration error handling test
- `src/Controller/SecurityController.php` — wrapped mailer.send() in try-catch, rollback user/token on TransportExceptionInterface
- `.env.deploy.example` — changed MAILER_DSN from devonly mailcatcher to template with real SMTP
- `.gitignore` — added playwright-report/, test-results/
- `Makefile` — added playwright-install, playwright-test, playwright-test-ui, playwright-test-headed targets
- `docs/playwright-e2e.md` (new) — user guide for running E2E tests
- `docs/mailer-production.md` (new) — configuration guide for production SMTP provider
- `docs/todo.md` — marked mailer task done, added Playwright+CI/CD as high priority
- `docs/ai-current-state.md` — added E2E Testing section, User Registration & Email Verification details
- `docs/operations-runbook.md` — added mailer troubleshooting section, Playwright E2E runbook
- `docs/project-rules.md` — added rule for graceful HTTP error handling in endpoints, AI Development Discipline requirement

**Rationale:**
- Production HTTP 500 on missing SMTP was blocking user experience; now returns friendly error + rollback.
- E2E tests provide confidence in registration flow and help detect mailer issues early.
- Documentation ensures operators know how to configure real SMTP and understand Playwright setup.

**Test Results:**
- `npm run pw:test` — 2/2 tests pass locally

---
